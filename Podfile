# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MA18FirebaseAuth' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MA18FirebaseAuth
  pod 'Firebase/Core'
  pod 'Firebase/Firestore'
  pod 'Firebase/Auth'

  target 'MA18FirebaseAuthTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MA18FirebaseAuthUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
